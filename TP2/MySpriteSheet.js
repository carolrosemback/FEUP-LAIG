class MySpriteSheet{

  constructor(scene, texture, sizeM, sizeN){
    this.scene = scene;
    this.texture = texture;
    this.column = sizeM;
    this.row = sizeN;
  }

  activateCellMN(column, row){}

  activateCellP(p){}

}
